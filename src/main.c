#include <math.h>  // ---------UDALiiiiiiiT'

#include "s21_exp.h"

int main() {
  double x;
  scanf("%lf", &x);
  printf("%Lf", s21_exp(x));
  printf("\n%lf", exp(x));  // ---------UDALiiiiiiiT'
  return 0;
}
